<% content_for :title, "Edit #{@bot.name}" %>

<div class="bg-white shadow sm:rounded-lg">
  <div class="px-4 py-5 sm:p-6">
    <div class="mb-4">
      <h3 class="text-base font-semibold leading-6 text-gray-900">
        Edit <%= @bot.name %>
      </h3>
    </div>

    <%= render "form", bot: @bot %>
  </div>
</div> 